#include "RenderTargetView.h"
#include <iostream>

namespace GraphicsModule
{
    /*RenderTargetView::RenderTargetView(const RenderTargetView& other)
    {
        memcpy(m_renderTargetView, other.m_renderTargetView, sizeof(ID3D11RenderTargetView));
    }*/
}